package com.example.plentifood.ui.utils

import android.content.Context
import android.content.pm.PackageManager

object ManifestUtils {
//    fun getApiKeyFromManifest(context: Context): String {
//        val applicationInfo = context.packageManager
//            .getApplicationInfo(context.packageName, PackageManager.GET_META_DATA)
//
//        return applicationInfo.metaData
//            .getString("com.google.android.geo.API_KEY")
//            ?: throw IllegalStateException("Google Maps API key missing in AndroidManifest")
//    }
}
